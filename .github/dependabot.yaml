version: 2
updates:
    -   package-ecosystem: uv
        directory: /
        schedule:
            interval: cron
            cronjob: 0 16 * * 1
        target-branch: dev
    -   package-ecosystem: github-actions
        directory: /
        schedule:
            interval: cron
            cronjob: 0 16 * * 1
        target-branch: dev
